<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.canccode.aca">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Required for all foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        Required on API 34+ when foregroundServiceType="specialUse".
        Without this the OS throws SecurityException on startForeground().
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LLMCodeAssistant"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            LlmService — foreground service that keeps the LLM inference coroutine alive
            when the screen is off or the app is backgrounded.

            foregroundServiceType="specialUse" is the correct type for long-running
            on-device CPU computation (LLM inference) that does not fit standard categories.
            It requires:
              1. FOREGROUND_SERVICE_SPECIAL_USE permission (declared above)
              2. The PROPERTY_SPECIAL_USE_FGS_SUBTYPE property below (required by Play Store)
              3. ServiceInfo.FOREGROUND_SERVICE_TYPE_SPECIAL_USE passed to startForeground()
                 on API 34+ (handled in LlmService.kt)
        -->
        <service
            android:name=".LlmService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="On-device LLM inference — keeps AI model active during background use" />
        </service>

    </application>

</manifest>
